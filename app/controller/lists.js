angular
.module("work")
.controller("lists", function($scope){
    /*$scope.lists = [];
    console.log($scope.lists.length);
    $scope.newList = function(){
        id = $scope.lists.length + 1;
        $scope.lists[id] = [];
        $scope.lists[id].id = id;
        $scope.lists[id].name = $("input", this).val();
        $scope.lists[id].items = [];
    }
    $scope.newItem = function(){
        id = $scope.lists[$(this).data("list")].items.length + 1;
        $scope.lists[$(this).data("list")].items[id] = [];
        $scope.lists[$(this).data("list")].items[id].name = $("input", this).val();
        $scope.lists[$(this).data("list")].items[id].done = false;
    }
    $scope.done = function(){
        $scope.lists[$(this).data("list")].items[$scope.lists[$(this).data("id")].items.length + 1].done = true;
    }*/
});